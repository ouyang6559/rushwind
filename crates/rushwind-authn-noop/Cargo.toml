[package]
name = "rushwind-authn-noop"
description = "Noop engine for the RushWind authentication contract: accepts every credential, mints none."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
rushwind-authn = { workspace = true }
