[package]
name = "rushwind-registry"
description = "Registration-only registry contract for RushWind services: Registrar trait, the extracted go-wind wire format, and key layout."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
rushwind-transport = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
