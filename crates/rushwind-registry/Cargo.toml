[package]
name = "rushwind-registry"
description = "Registry contract for RushWind services: Registrar and Discovery traits, the rush-wind wire format and key layouts."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
rushwind-transport = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
