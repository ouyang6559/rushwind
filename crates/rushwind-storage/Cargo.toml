[package]
name = "rushwind-storage"
description = "RushWind storage contracts: repository trait, paging, filtering, viewer scoping, auditing."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
# Intentionally empty: the contract layer must stay dependency-free so that
# every engine adapter can adopt it without locking users into a driver.
